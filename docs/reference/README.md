# Reference Specs

- `clockify-openapi.json` is the preferred, most up-to-date OpenAPI reference.
- `clockify-openapi.yaml` is kept for legacy tooling and diff comparisons.

If you update the spec, prefer the JSON file and keep the YAML in sync only if needed.
